image: tomcat:7.0-jdk8

services:
  - docker:dind

stages:
  - build
  - package
  - deploy

maven-build:
  cache:
    paths:
      - .m2/

  before_script:
    - echo '<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
                      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                      xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                              https://maven.apache.org/xsd/settings-1.0.0.xsd">
                  <localRepository>'$PWD'.m2</localRepository>
            </settings>' > $HOME/.m2/settings.xml

  image: mcr.microsoft.com/java/maven:8-zulu-debian9
  stage: build
  script: "mvn package"
  artifacts:
    paths:
      - combined/target/*.jar 

#docker-build:
#  stage: package
#  script:
#  - docker build -t aurimrv/tomcat-img ./web
#  - docker login -u $DOCKER_ID -p $DOCKER_TOKEN
#  - docker push aurimrv/tomcat-img
#  only:
#    - master